# Filter Lists Directory

Filter lists are NOT stored here — they are downloaded at runtime.
This file exists only to satisfy Flutter's asset bundle requirement for the directory.

See: lib/features/shield/data/filter_list_manager.dart
